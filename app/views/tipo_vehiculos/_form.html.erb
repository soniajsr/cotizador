<%= form_for(@tipo_vehiculo) do |f| %>
  <% if @tipo_vehiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_vehiculo.errors.count, "error") %> prohibited this tipo_vehiculo from being saved:</h2>

      <ul>
      <% @tipo_vehiculo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
